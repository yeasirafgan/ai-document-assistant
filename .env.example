# এটা একটা TEMPLATE — আসল key এখানে নেই (এই file টা GitHub এ যায়)।
# অন্য কেউ project clone করলে এটা দেখে নিজের .env বানাবে।
#
# আসল key থাকে .env file এ (যেটা .gitignore এ আছে, GitHub এ যায় না)।

ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here
